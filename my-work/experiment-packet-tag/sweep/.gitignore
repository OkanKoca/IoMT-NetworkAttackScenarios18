# Generated by run_sweep.py --outroot / build_dataset.py — regenerate from code, don't track.
# The tagged scenarios are deterministic per seed, so this whole tree rebuilds byte-identically:
#   run_sweep.py --outroot sweep  &&  build_dataset.py on the two manifests  &&  isolate_tag.py
raw/
out/
out_probes/
manifest.csv
manifest_probes.csv
sweep*.log
